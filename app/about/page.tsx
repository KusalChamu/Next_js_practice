// states, react hooks belongs to client side so it doesn't work on server side

"use client"

console.log("client or server?")

export default function About() {
    return (
        <div>about</div>
    )
}